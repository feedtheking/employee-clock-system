pluginManagement {
    repositories {
        google()          // Needed for Android Gradle plugin
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "EmployeeClockApp"
include(":app")
